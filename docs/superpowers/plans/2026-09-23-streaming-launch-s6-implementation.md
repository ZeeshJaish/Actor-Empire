# Streaming Launch S6 Implementation Plan

> **For agentic workers:** Execute inline in this user-owned checkout. The user approved S6 after S5 and previously requested inline phase work; do not dispatch subagents, create a worktree, commit, push, or mutate the user's real save. Track steps with checkboxes.

**Goal:** Establish a defensible release decision from one canonical career journey, save compatibility checks, the focused audit matrix, and responsive visual evidence.

**Architecture:** Use an in-memory synthetic `Player` for the transactional journey and isolated browser fixtures for presentation. Keep source-of-truth checks in canonical services, not cloned UI calculations. Classify failures by first failing boundary and whether affected code was changed during S0–S5. Only repair confirmed test drift or P0/P1 gameplay defects with a red-green regression; all other findings remain in the report.

**Tech Stack:** TypeScript, Node/esbuild audits, React/Vite, isolated in-app browser UI, existing save normalization and finance services.

**Spec:** `docs/superpowers/plans/2026-09-23-streaming-launch-stabilization-roadmap.md`, Phase S6.

## Global constraints

- Read-only with respect to the user's live browser storage, player save, remote services, and real payments.
- No new pricing, filing-energy, capacity, construction-duration, or competitor balance rule without a separate decision.
- Preserve the dirty worktree; do not stage or commit unrelated changes.
- A passing build is not a release sign-off. Record every audit exit status and any visual/career path not exercised.

---

### Task 1: Freeze evidence and classify existing failures

**Files:** `docs/superpowers/reports/2026-09-23-streaming-launch-s6.md` (create); `scripts/audit-streaming-finance-room.ts` and `scripts/audit-world-streaming-we{4,5,6}.ts` (inspect, modify only if a stale assertion is proven).

**Interfaces:** Existing `npm run audit:*` scripts are the baseline. The report's ledger must name the command, first failure, gameplay consequence, and disposition.

- [x] Record branch/dirty-worktree status and run the S0–S5, finance, WE4–7, save, construction, and weekly-loop audit set without editing gameplay.
- [x] Trace the finance 25M→26M and WE4–6 `const` assertions against canonical transaction/game-loop code; write down the root cause before any test correction.
- [x] Correct only proven audit drift, observe the original red result and the corrected green result, and continue through later assertions.

### Task 2: Canonical transactional journey and save boundaries

**Files:** `scripts/audit-streaming-regional-network-lifecycle.ts` (extend the existing canonical journey; no duplicate audit command). Services are modified only for a reproduced gameplay defect.

**Interfaces:** The audit consumes existing `Player`, founding, market filing, service configuration, infrastructure draft/rehearsal/commit, opening programme, weekly loop, and `normalizeOwnedStreamingPlatformState` APIs. It never reads browser storage.

- [x] Use the canonical incorporation, capital, market-plan and clearance actions in an in-memory journey. Separately verify the dashboard handoff and blank Build in an isolated browser career fixture.
- [x] Continue that journey through saved pricing, regional infrastructure, failed/held rehearsal evidence, quote, commission, construction, opening, and one weekly settlement. Separate focused audits exercise the actual routed-load rehearsal engine and signature gate.
- [x] JSON-serialize and normalize draft, commissioned, construction, and live states; exercise old city-era migration and live expansion.
- [x] Correct only proven audit drift and rerun the affected phase audits. No production gameplay change was indicated by these failures.

### Task 3: Responsive and interaction matrix

**Files:** `docs/superpowers/reports/2026-09-23-streaming-launch-s6.md` (matrix). Existing fixture files under `scripts/fixtures/` and `lab/` remain read-only. This browser-control path does not expose a screenshot-file writer; preserve exact inspected states and note that the 18-screen archive is incomplete.

**Interfaces:** Isolated browser session; never use the player's local-storage save. Use the existing lab's empty, configured, blocked, rehearsal, and agreement states where possible.

- [x] Inspect 30 isolated lab screen/viewport combinations at 393×600, 393×852, and 1280×800; explicitly record that they do not constitute a fresh 18-screen real-career archive.
- [x] Check document and Build-header overflow, visible non-finite values, market counter wrapping, a keyboard agreement disclosure, and the new-career market/capital/Build handoff. Console capture and real-device interaction are not verified.
- [x] No P0/P1 visual or interaction defect was reproduced in the inspected fixtures. Do not infer a real-career visual pass from that result.

### Task 4: Release gate and handoff

**Files:** `docs/superpowers/reports/2026-09-23-streaming-launch-s6.md`.

- [x] Rerun focused economics, migration/integrity, ledger/energy, map, rehearsal/commission, construction, weekly-loop, TypeScript (8GB heap), production build, and scoped diff checks.
- [x] Separate inherited audit drift from new failures and untested paths. With the real-career visual/device gate incomplete, mark release sign-off as not yet proven.
- [x] Present the release/merge decision to the user; do not commit, push, or merge without an explicit request.
