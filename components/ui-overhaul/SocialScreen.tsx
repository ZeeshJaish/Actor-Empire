import React from 'react';
import { BASE_CSS } from './theme';
import { Icon, Meter, Segmented, Sheet, Style, clamp, compactMoney, cssVar, money, useReveal } from './ui';
import type { SocialInteractionType, SocialUiGeneration, SocialUiHeir, SocialUiModel, SocialUiMove, SocialUiPerson } from '../../services/socialUiAdapter';
import type { Relationship } from '../../types';

interface SocialScreenProps extends SocialUiModel {
  onMove: (person: SocialUiPerson, action: SocialInteractionType) => void;
  onContinueAsChild: (child: Relationship) => void;
}

const CSS = `
.cx-head{margin-bottom:14px}.cx-triage{display:flex;align-items:center;gap:9px;margin-top:14px;padding:11px 13px;border-radius:14px;font-size:11.5px;font-weight:750;letter-spacing:-.01em;color:color-mix(in srgb,var(--c) 78%,#fff);background:color-mix(in srgb,var(--c) 9%,transparent);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--c) 24%,transparent)}.cx-triage svg{flex:none;color:var(--c)}.cx-triage b{color:var(--c)}
.cx-av{display:block;position:relative;width:52px;height:52px;flex:none}.cx-av svg{position:absolute;inset:0}.cx-face{position:absolute;inset:6px;border-radius:9px;overflow:hidden;display:grid;place-items:center;font-size:15px;font-weight:800;color:var(--c);background:color-mix(in srgb,var(--c) 15%,var(--card-2));box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--c) 26%,transparent)}.cx-face img{width:100%;height:100%;object-fit:cover;image-rendering:pixelated}.cx-name{display:block;font-size:16px;font-weight:750;letter-spacing:-.025em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-role{display:block;margin-top:3px;font-size:9px;font-weight:800;letter-spacing:.13em;color:var(--faint);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.cx-p{position:relative;width:100%;margin-top:9px;padding:13px;border-radius:19px;text-align:left;background:var(--card);box-shadow:inset 0 0 0 1px var(--line)}.cx-p-top{display:flex;align-items:center;gap:12px;width:100%;text-align:left}.cx-p-t{flex:1;min-width:0}.cx-p-b{flex:none;text-align:right}.cx-p-b b{display:block;font-size:19px;font-weight:800;letter-spacing:-.04em;line-height:1;color:var(--c)}.cx-fuse{display:flex;align-items:center;gap:6px;margin-top:12px;padding:8px 11px;border-radius:11px;font-size:10px;font-weight:800;letter-spacing:.07em;color:var(--faint);background:rgba(255,255,255,.035);box-shadow:inset 0 0 0 1px var(--hair)}.cx-fuse.is-warm{color:var(--gold-hi);background:rgba(248,168,16,.08);box-shadow:inset 0 0 0 1px rgba(248,168,16,.22)}.cx-fuse.is-cold{color:var(--alert);background:rgba(255,92,92,.09);box-shadow:inset 0 0 0 1px rgba(255,92,92,.26)}.cx-fuse em{font-style:normal;margin-left:auto;font-weight:750;letter-spacing:0;opacity:.8}.cx-quick{display:flex;gap:7px;margin-top:9px}.cx-q{flex:1;display:flex;align-items:center;justify-content:center;gap:6px;height:38px;border-radius:12px;font-size:11px;font-weight:800;letter-spacing:-.01em;color:color-mix(in srgb,var(--c) 84%,#fff);background:color-mix(in srgb,var(--c) 11%,transparent);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--c) 26%,transparent);transition:transform .12s ease}.cx-q:active{transform:scale(.96)}.cx-q:disabled{opacity:.4}.cx-q u{text-decoration:none;font-size:9px;font-weight:800;opacity:.66}.cx-more{flex:none;width:38px;height:38px;border-radius:12px;display:grid;place-items:center;color:var(--dim);background:rgba(255,255,255,.04);box-shadow:inset 0 0 0 1px var(--hair)}.cx-rest{margin-top:11px;font-size:10.5px;font-weight:750;letter-spacing:.04em;color:var(--faint);text-align:center}
.cx-row{width:100%;margin-top:7px;padding:10px 13px;border-radius:15px;text-align:left;display:flex;align-items:center;gap:11px;background:var(--card);box-shadow:inset 0 0 0 1px var(--line-2)}.cx-row:active{background:var(--card-2)}.cx-row .cx-av{width:38px;height:38px}.cx-row .cx-face{inset:4px;border-radius:7px;font-size:12px}.cx-row-t{flex:1;min-width:0}.cx-row .cx-name{font-size:14px}.cx-row .cx-role{margin-top:2px;font-size:8.5px;letter-spacing:.11em}.cx-row-b{flex:none;display:flex;align-items:center;gap:9px}.cx-row-v{font-size:15px;font-weight:800;letter-spacing:-.035em;color:var(--c)}.cx-dot{width:7px;height:7px;border-radius:50%;background:var(--f);box-shadow:0 0 7px var(--f)}
.cx-hero{display:flex;align-items:center;gap:14px;margin-top:2px;padding:16px;border-radius:22px;background:var(--card);box-shadow:inset 0 0 0 1px var(--line)}.cx-crown{width:52px;height:52px;flex:none;border-radius:18px;display:grid;place-items:center;color:var(--gold-hi);background:rgba(248,168,16,.11);box-shadow:inset 0 0 0 1px var(--line)}.cx-hero-t{flex:1;min-width:0}.cx-hero-v{display:block;margin-top:4px;font-size:34px;font-weight:800;letter-spacing:-.05em;line-height:1;color:var(--gold-hi)}.cx-hero-d{display:block;margin-top:7px;font-size:11px;font-weight:650;color:var(--faint);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-duo{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin-top:9px}.cx-duo-c{min-width:0;padding:14px;border-radius:18px;background:var(--card);box-shadow:inset 0 0 0 1px var(--line)}.cx-duo-v{margin-top:6px;font-size:30px;font-weight:800;letter-spacing:-.05em;line-height:1}.cx-duo-n{margin-top:8px;font-size:10.5px;font-weight:650;line-height:1.45;color:var(--faint)}.cx-line{position:relative;margin-top:10px;padding-left:34px}.cx-line::before{content:"";position:absolute;left:13px;top:8px;bottom:26px;width:2px;border-radius:1px;background:linear-gradient(180deg,var(--gold),rgba(248,168,16,.12))}.cx-gen{position:relative;padding-bottom:14px}.cx-node{position:absolute;left:-34px;top:0;width:28px;height:28px;border-radius:10px;display:grid;place-items:center;font-size:10px;font-weight:800;letter-spacing:-.02em;color:#241606;background:var(--gold);box-shadow:0 0 0 4px var(--bg)}.cx-node.is-empty{color:var(--faint);background:var(--card-2);box-shadow:0 0 0 4px var(--bg),inset 0 0 0 1px var(--hair)}.cx-gen-n{font-size:14px;font-weight:800;letter-spacing:-.025em;line-height:1.5}.cx-gen-d{margin-top:2px;font-size:10.5px;font-weight:650;color:var(--faint);line-height:1.45}.cx-heir{margin-top:11px;padding:13px;border-radius:18px;background:var(--card);box-shadow:inset 0 0 0 1px var(--line)}.cx-heir-top{display:flex;align-items:center;gap:11px}.cx-heir-f{width:40px;height:40px;flex:none;border-radius:9px;overflow:hidden;display:grid;place-items:center;font-size:15px;font-weight:800;color:var(--gold-hi);text-transform:uppercase;background:rgba(248,168,16,.13);box-shadow:inset 0 0 0 1px var(--line)}.cx-heir-f img{width:100%;height:100%;object-fit:cover;image-rendering:pixelated}.cx-heir-n{font-size:15.5px;font-weight:750;letter-spacing:-.025em;text-transform:capitalize}.cx-heir-s{margin-left:auto;flex:none;text-align:right}.cx-heir-s b{display:block;font-size:17px;font-weight:800;letter-spacing:-.04em;line-height:1;color:var(--gold-hi)}.cx-hstats{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin-top:13px;padding-top:13px;border-top:1px solid var(--line-2)}.cx-hs{min-width:0}.cx-hs b{display:block;margin-top:4px;font-size:13px;font-weight:800;letter-spacing:-.03em;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-continue{margin-top:12px;height:38px}.cx-next{margin-top:11px;padding:22px 16px;border-radius:18px;text-align:center;box-shadow:inset 0 0 0 1px var(--hair)}.cx-next svg{margin:0 auto;color:var(--ghost)}.cx-next b{display:block;margin-top:10px;font-size:13px;font-weight:750;letter-spacing:-.02em;color:var(--dim)}.cx-next p{margin-top:5px;font-size:11px;line-height:1.55;color:var(--faint)}
.cx-file{display:flex;align-items:center;gap:13px}.cx-file-t{min-width:0}.cx-file-n{margin-top:5px;font-size:21px;font-weight:800;letter-spacing:-.03em;line-height:1.05}.cx-file-b{font-size:12px;line-height:1.6;color:var(--dim)}.cx-stats{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:14px}.cx-stat{min-width:0;padding:10px 9px 11px;border-radius:14px;background:var(--card-2);box-shadow:inset 0 0 0 1px var(--hair)}.cx-stat b{display:block;margin:7px 0 8px;font-size:17px;font-weight:800;letter-spacing:-.03em;line-height:1.1;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-moves{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px}.cx-move{min-width:0;padding:12px;border-radius:16px;text-align:left;background:var(--card-2);box-shadow:inset 0 0 0 1px var(--hair);transition:transform .12s ease}.cx-move:active{transform:scale(.97)}.cx-move:disabled{opacity:.42;cursor:default}.cx-move.is-danger{box-shadow:inset 0 0 0 1px rgba(255,92,92,.25)}.cx-move-top{display:flex;align-items:flex-start;gap:8px}.cx-move-i{width:34px;height:34px;flex:none;border-radius:12px;display:grid;place-items:center;color:var(--c);background:color-mix(in srgb,var(--c) 13%,transparent);box-shadow:inset 0 0 0 1px color-mix(in srgb,var(--c) 26%,transparent)}.cx-move-cost{margin-left:auto;text-align:right}.cx-move-e{display:flex;align-items:center;justify-content:flex-end;gap:2px;font-size:10.5px;font-weight:800;color:var(--gold-hi)}.cx-move-m{display:block;margin-top:3px;font-size:9.5px;font-weight:800;letter-spacing:.03em}.cx-move-m.is-free{color:var(--faint)}.cx-move-m.is-paid{color:var(--money)}.cx-move-m.is-pick{color:var(--looks)}.cx-move-n{display:block;margin-top:10px;font-size:13px;font-weight:750;letter-spacing:-.02em}.cx-move-g{display:block;margin-top:4px;font-size:9px;font-weight:800;letter-spacing:.08em;color:var(--mood)}.cx-move-d{display:block;margin-top:6px;font-size:10.5px;font-weight:650;line-height:1.45;color:var(--faint)}.cx-back{display:flex;align-items:center;gap:7px;margin-bottom:10px;font-size:10px;font-weight:800;letter-spacing:.08em;color:var(--gold-hi)}.cx-transfer{padding:14px;border-radius:17px;background:rgba(248,168,16,.07);box-shadow:inset 0 0 0 1px rgba(248,168,16,.2)}.cx-transfer p{font-size:11.5px;line-height:1.55;color:var(--dim)}.cx-transfer-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:12px}.cx-transfer-stat{min-width:0;padding:11px;border-radius:14px;background:var(--card-2);box-shadow:inset 0 0 0 1px var(--hair)}.cx-transfer-stat b{display:block;margin-top:6px;font-size:16px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-transfer-note{margin-top:12px;font-size:10.5px;line-height:1.5;color:var(--faint)}
.cx-careers{display:grid;gap:7px}.cx-career{display:grid;grid-template-columns:38px minmax(0,1fr) auto;align-items:center;gap:10px;padding:11px 12px;border-radius:15px;background:var(--card);box-shadow:inset 0 0 0 1px var(--line-2)}.cx-career-f{width:38px;height:38px;border-radius:9px;overflow:hidden;display:grid;place-items:center;font-size:13px;font-weight:800;color:var(--gold-hi);background:rgba(248,168,16,.12)}.cx-career-f img{width:100%;height:100%;object-fit:cover}.cx-career-t{min-width:0}.cx-career-t b,.cx-career-t span{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.cx-career-t b{font-size:13px}.cx-career-t span{margin-top:3px;font-size:9.5px;color:var(--faint)}.cx-career-s{text-align:right}.cx-career-s b{display:block;font-size:9px;letter-spacing:.08em;color:var(--mood)}.cx-career-s span{display:block;margin-top:4px;font-size:8.5px;color:var(--faint)}
@media(max-width:374px){.ae-scroll{padding-left:12px;padding-right:12px}.ae .ae-seg-b{gap:3px;font-size:8px;letter-spacing:.07em}.ae-seg-n{display:none}.cx-p{padding:11px}.cx-p-top{gap:9px}.cx-name{font-size:14.5px}.cx-q{font-size:9.5px;gap:4px}.cx-duo,.cx-moves,.cx-transfer-grid{gap:6px}.cx-duo-c{padding:11px}.cx-duo-v{font-size:26px}.cx-move{padding:10px}.cx-move-n{font-size:12px}.cx-stats{gap:5px}.cx-stat{padding-left:7px;padding-right:7px}.cx-stat b{font-size:14px}}
`;

const RING_BOX = 52;
const RING_PAD = 1.75;
const RING_RADIUS = 13;
const ringPath = (box = RING_BOX, pad = RING_PAD, radius = RING_RADIUS) => {
  const x = pad;
  const y = pad;
  const size = box - pad * 2;
  const center = x + size / 2;
  return [`M`, center, y, `H`, x + size - radius, `A`, radius, radius, 0, 0, 1, x + size, y + radius, `V`, y + size - radius, `A`, radius, radius, 0, 0, 1, x + size - radius, y + size, `H`, x + radius, `A`, radius, radius, 0, 0, 1, x, y + size - radius, `V`, y + radius, `A`, radius, radius, 0, 0, 1, x + radius, y, `Z`].join(' ');
};
const ringLength = 4 * (RING_BOX - RING_PAD * 2 - 2 * RING_RADIUS) + 2 * Math.PI * RING_RADIUS;
const bondHue = (bond: number) => bond >= 85 ? 'var(--mood)' : bond >= 50 ? 'var(--gold-hi)' : 'var(--alert)';
const heat = (weeks: number) => weeks >= 52 ? 'is-cold' : weeks >= 12 ? 'is-warm' : '';
const heatHue = (weeks: number) => weeks >= 52 ? 'var(--alert)' : weeks >= 12 ? 'var(--gold-hi)' : 'var(--mood)';
const since = (weeks: number) => weeks >= 104 ? `${Math.round(weeks / 52)} years since contact` : weeks >= 52 ? '1 year since contact' : weeks >= 8 ? `${weeks} weeks since contact` : weeks <= 0 ? 'You spoke this week' : `${weeks} weeks since contact`;
const shortSince = (weeks: number) => weeks >= 104 ? `${Math.round(weeks / 52)}y` : weeks >= 52 ? '1y' : weeks <= 0 ? 'now' : `${weeks}w`;
const verdict = (weeks: number) => weeks >= 52 ? 'GOING COLD' : weeks >= 12 ? 'GOING QUIET' : weeks <= 0 ? 'WARM' : 'OK';

function Ring({ person, progress = 1 }: { person: SocialUiPerson; progress?: number }) {
  const hue = bondHue(person.bond);
  return <span className="cx-av" style={cssVar('--c', person.accent || hue)}>
    <svg viewBox="0 0 52 52"><path d={ringPath()} fill="none" stroke="rgba(255,255,255,.09)" strokeWidth="3" /><path d={ringPath()} fill="none" stroke={hue} strokeWidth="3" strokeLinecap="round" strokeDasharray={ringLength} strokeDashoffset={ringLength * (1 - clamp(person.bond) / 100 * progress)} /></svg>
    <span className="cx-face">{person.avatarUrl ? <img src={person.avatarUrl} alt="" /> : person.relationship.relation === 'Pet' ? (person.relationship.petEmoji || '🐾') : person.name.charAt(0)}</span>
  </span>;
}

function MoveCard({ move, onClick }: { move: SocialUiMove; onClick: () => void; key?: React.Key }) {
  return <button className={`cx-move${move.danger ? ' is-danger' : ''}`} style={cssVar('--c', move.color)} onClick={onClick} disabled={move.disabled} aria-label={move.name}>
    <span className="cx-move-top"><span className="cx-move-i"><Icon name={move.icon} size={17} /></span><span className="cx-move-cost">{move.pickLabel ? <span className="cx-move-m is-pick">{move.pickLabel}</span> : <><span className="cx-move-e"><Icon name="bolt" size={9} />−{move.energy}</span><span className={`cx-move-m ${move.cost ? 'is-paid' : 'is-free'}`}>{move.cost ? `−${compactMoney(move.cost)}` : 'FREE'}</span></>}</span></span>
    <span className="cx-move-n">{move.name}</span>
    {(move.resultLabel || move.gain !== undefined) && <span className="cx-move-g">{move.resultLabel || `+${move.gain} BOND`}</span>}
    <span className="cx-move-d">{move.note}</span>
  </button>;
}

function HeirCard({ member, onContinue }: { member: SocialUiHeir; onContinue: (member: SocialUiHeir) => void; key?: React.Key }) {
  return <div className="cx-heir"><div className="cx-heir-top"><span className="cx-heir-f">{member.avatarUrl ? <img src={member.avatarUrl} alt="" /> : member.name.charAt(0)}</span><div><div className="cx-heir-n">{member.name}</div><div className="cx-gen-d">{member.age !== undefined ? `Age ${member.age} · ` : ''}{compactMoney(member.netWorth)} net worth</div></div><span className="cx-heir-s"><b>{member.score}</b><span className="ae-lab" style={{ marginTop: 4 }}>Score</span></span></div><div className="cx-hstats"><div className="cx-hs"><span className="ae-lab">Projects</span><b>{member.projects}</b></div><div className="cx-hs"><span className="ae-lab">Awards</span><b>{member.awards}</b></div><div className="cx-hs"><span className="ae-lab">Fame</span><b style={{ color: 'var(--gold-hi)' }}>{member.fame}</b></div><div className="cx-hs"><span className="ae-lab">Businesses</span><b style={{ color: 'var(--acting)' }}>{member.businesses}</b></div><div className="cx-hs" style={{ gridColumn: 'span 2' }}><span className="ae-lab">Net worth</span><b style={{ color: 'var(--money)' }}>{money(member.netWorth)}</b></div></div>{member.continuationRelationship && <button className="ae-btn cx-continue" disabled={!member.continuationEligible} onClick={() => onContinue(member)}>{member.continuationEligible ? `Continue as ${member.name}` : 'Heir must be 18'}</button>}</div>;
}

export default function SocialScreen(props: SocialScreenProps) {
  const reveal = useReveal();
  const [tab, setTab] = React.useState<'people' | 'legacy'>('people');
  const [openId, setOpenId] = React.useState<string | null>(null);
  const [submenu, setSubmenu] = React.useState<SocialUiMove | null>(null);
  const [legacyCandidate, setLegacyCandidate] = React.useState<SocialUiHeir | null>(null);
  const sorted = props.people.slice().sort((left, right) => right.lastTouchWeeks - left.lastTouchWeeks || left.bond - right.bond);
  const quiet = sorted.filter(person => person.lastTouchWeeks >= 12 && person.relationship.relation !== 'Deceased Parent');
  const cold = quiet.filter(person => person.lastTouchWeeks >= 52).length;
  const cards = quiet.slice(0, 3);
  const lifted = new Set(cards.map(person => person.id));
  const groupNames = props.people.map(person => person.group).filter((group, index, groups) => groups.indexOf(group) === index);
  const groups = groupNames.map(name => ({ name, total: props.people.filter(person => person.group === name).length, list: sorted.filter(person => person.group === name && !lifted.has(person.id)) }));
  const open = props.people.find(person => person.id === openId) || null;
  const ruler = props.generations.find(generation => generation.n === props.currentGeneration)?.members[0];

  const openPerson = (person: SocialUiPerson) => { setSubmenu(null); setOpenId(person.id); };
  const selectMove = (person: SocialUiPerson, selectedMove: SocialUiMove) => {
    if (selectedMove.choices?.length) { setSubmenu(selectedMove); return; }
    if (!selectedMove.action || selectedMove.disabled) return;
    props.onMove(person, selectedMove.action);
    setOpenId(null);
    setSubmenu(null);
  };
  const startContinuation = (member: SocialUiHeir) => { if (member.continuationEligible) setLegacyCandidate(member); };

  return <div className="ae" data-ui="actor-empire-social-overhaul"><Style css={BASE_CSS} /><Style css={CSS} /><div className="ae-bg" /><div className="ae-scroll">
    <header className="cx-head ae-in"><h1 className="ae-h1">{tab === 'people' ? 'Connections' : 'Legacy'}</h1><p className="ae-sub">{tab === 'people' ? 'Bonds decay every week you ignore them' : 'Track your bloodline generation by generation'}</p></header>
    <div className="ae-in" style={cssVar('--d', '.04s')}><Segmented items={[{ key: 'people' as const, label: 'CONNECTIONS', icon: 'users' as const, count: props.people.length }, { key: 'legacy' as const, label: 'LEGACY', icon: 'crown' as const, count: props.generations.length }]} active={tab} onChange={setTab} /></div>

    {tab === 'people' && <div className="ae-in">
      <div className="cx-triage" style={cssVar('--c', cold ? 'var(--alert)' : quiet.length ? 'var(--gold-hi)' : 'var(--mood)')}><Icon name={quiet.length ? 'flame' : 'check'} size={15} sw={2.2} />{quiet.length === 0 ? <span>Every bond is warm. Nothing needs you this week.</span> : <span><b>{quiet.length}</b> of {props.people.length} {quiet.length === 1 ? 'bond has' : 'bonds have'} gone quiet{cold ? <> — <b>{cold}</b> close to breaking</> : null}</span>}</div>
      {cards.length > 0 && <><div className="ae-sec"><span className="ae-lab">Needs you</span><span className="ae-sec-rule" /><span className="ae-lab">Rotten first</span></div>{cards.map(person => { const quick = person.moves.filter(item => !item.choices && !item.danger).slice(0, 2); return <div className="cx-p" key={person.id} style={cssVar('--c', bondHue(person.bond))}><button className="cx-p-top" onClick={() => openPerson(person)} aria-label={`Open ${person.name}`}><Ring person={person} progress={reveal} /><span className="cx-p-t"><span className="cx-name">{person.name}</span><span className="cx-role">{person.role} · {person.status}</span></span><span className="cx-p-b"><b>{Math.round(person.bond * reveal)}</b><span className="ae-lab" style={{ marginTop: 4 }}>Bond</span></span></button><div className={`cx-fuse ${heat(person.lastTouchWeeks)}`}><Icon name="clock" size={12} sw={2.2} />{since(person.lastTouchWeeks)}<em>{verdict(person.lastTouchWeeks)}</em></div><div className="cx-quick">{quick.map(item => <button className="cx-q" key={item.id} style={cssVar('--c', item.color)} disabled={item.disabled} onClick={() => selectMove(person, item)}><Icon name={item.icon} size={13} />{item.name}<u>{item.energy}E</u></button>)}<button className="cx-more" onClick={() => openPerson(person)} aria-label={`All moves for ${person.name}`}><Icon name="chevronRight" size={15} sw={2.2} /></button></div></div>; })}{quiet.length > cards.length && <p className="cx-rest">+{quiet.length - cards.length} more have gone quiet — they’re in the list below</p>}</>}
      {groups.map(group => <section key={group.name}><div className="ae-sec"><span className="ae-lab">{group.name}</span><span className="ae-sec-rule" /><span className="ae-lab">{group.total}</span></div>{group.list.length === 0 && <p className="cx-rest" style={{ marginTop: 0 }}>Everyone here needs you above.</p>}{group.list.map(person => <button className="cx-row" key={person.id} style={cssVar('--c', bondHue(person.bond))} onClick={() => openPerson(person)}><Ring person={person} /><span className="cx-row-t"><span className="cx-name">{person.name}</span><span className="cx-role">{person.role} · {shortSince(person.lastTouchWeeks)} ago</span></span><span className="cx-row-b"><span className="cx-row-v">{Math.round(person.bond)}</span><span className="cx-dot" style={cssVar('--f', heatHue(person.lastTouchWeeks))} /><Icon name="chevronRight" size={14} sw={2.2} /></span></button>)}</section>)}
      {props.people.length === 0 && <div className="cx-next"><Icon name="users" size={30} /><b>No connections yet</b><p>Your relationships will appear here as your life and career grow.</p></div>}
    </div>}

    {tab === 'legacy' && <div className="ae-in">
      <div className="ae-sec"><span className="ae-lab">Dynasty</span><span className="ae-sec-rule" /><span className="ae-lab">All generations</span></div>
      <section className="cx-hero"><span className="cx-crown"><Icon name="crown" size={25} /></span><span className="cx-hero-t"><span className="ae-lab">Dynasty score</span><b className="cx-hero-v">{Math.round(props.dynastyScore * reveal)}</b><span className="cx-hero-d">{ruler ? `${ruler.name} rules · ${compactMoney(ruler.netWorth)} net worth` : 'No ruler yet'}</span></span></section>
      <div className="cx-duo"><div className="cx-duo-c"><span className="ae-lab">Current generation</span><div className="cx-duo-v" style={{ color: 'var(--gold-hi)' }}>{props.currentGeneration}</div><p className="cx-duo-n">{ruler ? `${ruler.name} leads the family right now.` : 'No ruler yet.'}</p></div><div className="cx-duo-c"><span className="ae-lab">Heirs in line</span><div className="cx-duo-v" style={{ color: props.heirsInLine ? 'var(--mood)' : 'var(--ghost)' }}>{props.heirsInLine}</div><p className="cx-duo-n">{props.heirsInLine ? `${props.heirsInLine} ${props.heirsInLine === 1 ? 'heir' : 'heirs'} waiting to inherit.` : 'The line ends with you until you have a child.'}</p></div></div>
      {props.careerMembers.length > 0 && <><div className="ae-sec"><span className="ae-lab">Family careers</span><span className="ae-sec-rule" /><span className="ae-lab">After the handoff</span></div><div className="cx-careers">{props.careerMembers.map(member => <div className="cx-career" key={member.id}><span className="cx-career-f">{member.avatarUrl ? <img src={member.avatarUrl} alt="" /> : member.name.charAt(0)}</span><span className="cx-career-t"><b>{member.name}</b><span>{member.detail}</span></span><span className="cx-career-s"><b>{member.status.toUpperCase()}</b><span>G{member.generation} · AGE {member.age}</span></span></div>)}</div></>}
      <div className="ae-sec"><span className="ae-lab">Bloodline</span><span className="ae-sec-rule" /><span className="ae-lab">{props.generations.length} recorded</span></div>
      <div className="cx-line">{props.generations.map((generation: SocialUiGeneration) => <div className="cx-gen" key={`${generation.n}-${generation.note}`}><span className="cx-node">G{generation.n}</span><div className="cx-gen-n">{generation.label}</div><div className="cx-gen-d">{generation.note}</div>{generation.members.map(member => <HeirCard key={member.id} member={member} onContinue={startContinuation} />)}</div>)}<div className="cx-gen"><span className="cx-node is-empty"><Icon name="plus" size={14} sw={2.4} /></span><div className="cx-gen-n" style={{ color: 'var(--faint)' }}>Generation {Math.max(props.currentGeneration, ...props.generations.map(item => item.n)) + 1}</div><div className="cx-gen-d">Waiting on a future heir</div><div className="cx-next"><Icon name="baby" size={30} sw={1.5} /><b>The next generation is unwritten</b><p>Eligible children can take over from their Legacy card.</p></div></div></div>
    </div>}
  </div>

  <Sheet open={Boolean(open)} onClose={() => { setOpenId(null); setSubmenu(null); }} head={open ? <div className="cx-file"><Ring person={open} /><div className="cx-file-t"><span className="ae-lab" style={{ color: 'var(--gold)' }}>{submenu ? submenu.name : 'Relationship file'}</span><div className="cx-file-n">{open.name}</div><span className="ae-lab" style={{ marginTop: 6 }}>{open.role}{open.age !== undefined ? ` · AGE ${open.age}` : ''}</span></div></div> : undefined}>{open && <>{submenu && <button className="cx-back" onClick={() => setSubmenu(null)}><Icon name="back" size={13} />ALL ACTIONS</button>}{!submenu && <><p className="cx-file-b">{open.blurb}</p><div className="cx-stats"><div className="cx-stat"><span className="ae-lab">Bond</span><b style={{ color: bondHue(open.bond) }}>{Math.round(open.bond)}</b><Meter value={open.bond} color={bondHue(open.bond)} thin /></div><div className="cx-stat"><span className="ae-lab">Status</span><b>{open.status}</b></div><div className="cx-stat"><span className="ae-lab">Last touch</span><b style={{ fontSize: 14, color: heatHue(open.lastTouchWeeks) }}>{open.lastTouchWeeks >= 104 ? `${Math.round(open.lastTouchWeeks / 52)}y ago` : open.lastTouchWeeks <= 0 ? 'This week' : `${open.lastTouchWeeks}w ago`}</b></div></div></>}<div className="ae-sec"><span className="ae-lab">{submenu ? 'Choose route' : 'Social actions'}</span><span className="ae-sec-rule" /><span className="ae-lab">{props.energy}E · {compactMoney(props.money)}</span></div><div className="cx-moves">{(submenu?.choices || open.moves).map(item => <MoveCard key={item.id} move={item} onClick={() => selectMove(open, item)} />)}</div>{open.moves.length === 0 && <div className="cx-next"><Icon name="crown" size={26} /><b>Preserved in memory</b><p>This bond is part of the family legacy and has no weekly actions.</p></div>}</>}</Sheet>

  <Sheet open={Boolean(legacyCandidate)} onClose={() => setLegacyCandidate(null)} eyebrow="Legacy transfer" eyebrowIcon="crown" title={legacyCandidate ? `Continue as ${legacyCandidate.name}` : ''}>{legacyCandidate && <><div className="cx-transfer"><p>Retire the current character and continue the same game as this heir. This is a permanent generational handoff.</p><div className="cx-transfer-grid"><div className="cx-transfer-stat"><span className="ae-lab">Cash after tax</span><b>{compactMoney(props.inheritance.inheritedMoney)}</b><span className="cx-gen-d">−{compactMoney(props.inheritance.moneyTaxPaid)}</span></div><div className="cx-transfer-stat"><span className="ae-lab">Shares after tax</span><b>{props.inheritance.inheritedShares.toLocaleString()}</b><span className="cx-gen-d">−{props.inheritance.sharesTaxPaid.toLocaleString()} shares</span></div></div><p className="cx-transfer-note">Assets and businesses transfer without inheritance tax. Government tax rate: {Math.round(props.inheritance.taxRate * 100)}%.</p></div><button className="ae-btn" style={{ marginTop: 14 }} onClick={() => { if (legacyCandidate.continuationRelationship) props.onContinueAsChild(legacyCandidate.continuationRelationship); setLegacyCandidate(null); }}><Icon name="crown" size={15} />Confirm legacy shift</button></>}</Sheet>
  </div>;
}
